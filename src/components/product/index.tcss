.product-badge {
  font-size: 10px;
  background: #ff424e;
}
.product-old-price {
  font-size: 10px;
  text-decoration-line: line-through;
}
.product image,
.product-image-skeleton {
  height: 0;
  width: auto;
  padding-bottom: 100%;
}